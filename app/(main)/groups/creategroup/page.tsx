import AddGroup from "@/components/group/AddGroup";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AddGroup />
    </div>
  );
};

export default page;
